<%= form_with(model: church, local: true) do |form| %>
  <% if church.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(church.errors.count, "error") %> prohibited this church from being saved:</h2>

      <ul>
      <% church.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :church_name %>
    <%= form.text_field :church_name %>
  </div>

  <div class="field">
    <%= form.label :pastor_name %>
    <%= form.text_field :pastor_name %>
  </div>

  <div class="field">
    <%= form.label :mens_leader %>
    <%= form.text_field :mens_leader %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
